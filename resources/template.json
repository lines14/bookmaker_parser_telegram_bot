{
   "firstMatch": {
        "date": "{{ date }}",
        "time": "{{ time }}",
        "rates": {
            "firstTeam": {
                "name": "{{ first_name }}",
                "rate": "{{ first_rate }}"
            },
            "draw": {
                "rate": "{{ draw_rate }}"
            },
            "secondTeam": {
                "name": "{{ second_name }}",
                "rate": "{{ second_rate }}"
            }
        }
   }
}